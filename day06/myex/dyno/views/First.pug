html
   head
      title=name
   body
      h1=name
      a(href = url) URL

Change the above first.pug as below
html
   head
      title = name
   body
      h1 Greetings from #{name}
      a(href = url) URL


